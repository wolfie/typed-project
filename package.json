{
  "name": "typed-package",
  "license": "ISC",
  "private": true,
  "scripts": {
    "start": "concurrently -c green,blue -n package: yarn:start:*",
    "start:client": "yarn workspace typed-project-client start",
    "start:server": "yarn workspace typed-project-server start",
    "lint": "yarn workspaces run lint"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^7.5.0",
    "prettier": "^3.0.0-alpha.4",
    "typescript": "^4.4.2"
  }
}
